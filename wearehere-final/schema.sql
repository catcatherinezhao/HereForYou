drop table if exists records;
    create table records (
    user_name text,
    perp_name text, 
    perp_descrip text, 
    location text, 
    inc_date text, 
    inc_time text, 
    incident text, 
    contact text);

